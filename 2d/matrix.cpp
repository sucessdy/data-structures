#include <iostream>
#include <vector>
using namespace std;

vector<int> Binary_search (vector<vector<int , int>> &) { 

}

vector<int>  matrixSearch(vector<vector<int, int>> & arr , int target ) { 
int row = arr.size() ; 
int col  = arr[0].size() ; 
while ( i < row && j >= 0) { 
   if  
}
}
int main() {
    
    return 0;
}