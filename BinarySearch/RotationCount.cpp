#include <iostream>
#include <vector>
using namespace std;

int RototionCount(vector<int> &nums) { 
int st = 0; 
int end = nums.size() -1; 
while ( st <= end) { 
    int mid = st + ( end-st) / 2; 
    if ( arr[mid] < arr[mid +1 ]){ 
        
    }
}
}
int main() {
    
    return 0;
}